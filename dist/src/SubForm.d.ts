import React from 'react';
declare type State = {
    expanded: boolean;
};
declare type Props = {
    name: string;
};
export default class SubForm extends React.Component<Props, State> {
    constructor(props: Props);
    render(): JSX.Element;
}
export {};
